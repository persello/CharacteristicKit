import XCTest
@testable import CharacteristicKit

final class CharacteristicKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CharacteristicKit().text, "Hello, World!")
    }
}
